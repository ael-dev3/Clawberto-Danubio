# HyperEVM RPC. Defaults to the public Hyperliquid endpoint when unset.
HYPEREVM_RPC_URL=https://rpc.hyperliquid.xyz/evm

# Live launch private key. Keep this out of git and fund it with native HYPE
# for launchFee() plus gas before running `npm run launch`.
HYPEREVM_EXEC_PRIVATE_KEY=0x...
